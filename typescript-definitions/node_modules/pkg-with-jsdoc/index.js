/**
 * Sorts array of numbers
 * @param {number[]} A Array to be sorted
 * @param {boolean} [reverse=false] Whether sorting should be reversed
 * @returns {number[]}
 */
function sort(A, reverse = false) {
  const sorted = A.sort()
  return reverse ? sorted.reverse() : sorted
}

module.exports = sort
